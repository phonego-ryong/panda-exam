// 'use client';

import { normalTextStyle } from '@/components/text/NomalText';
import { css } from '../../../styled-system/css';

export function RedText() {
  return <span className={css(normalTextStyle)}>normalText</span>;
}
